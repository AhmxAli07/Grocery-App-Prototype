import 'package:flutter/material.dart';

class ImageUPloadSreen extends StatelessWidget {
  const ImageUPloadSreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Image Upload')));
  }
}
